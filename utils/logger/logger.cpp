#include "logger.hpp"
#include <fmt/core.h>

namespace logger{
    logger::logger(const std::string& fmt, const std::string& path){

    }

    bool logger::ok(){


    }

    template<typename... T>
    bool logger::write(T &&... args){
        
    }

    bool logger::close(){
        
    }
}